using System.Collections;
using UnityEngine;

public class Arimatika : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        int A = 3 * 5;
        Debug.Log(A);
    }
}
